name: advanced_field_app
description: Advanced relay_sync example wired to local core and Flutter packages.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  relay_sync:
    path: ../../packages/relay_sync
  relay_sync_flutter:
    path: ../../packages/relay_sync_flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
