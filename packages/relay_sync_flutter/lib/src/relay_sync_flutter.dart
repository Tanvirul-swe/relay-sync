/// Top-level integration handle for the relay_sync_flutter package.
///
/// Instantiate once to confirm the Flutter integration layer is wired in.
/// Useful as a runtime type marker in logs and debug UIs.
class RelaySyncFlutter {
  const RelaySyncFlutter();
}
