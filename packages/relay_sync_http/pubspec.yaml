name: relay_sync_http
description: package:http adapter for relay_sync.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  http: ^1.2.1
  relay_sync:
    path: ../relay_sync

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
