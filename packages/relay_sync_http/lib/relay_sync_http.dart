/// HTTP adapter APIs for relay_sync.
library relay_sync_http;

/// Placeholder HTTP adapter scaffold.
class RelaySyncHttpAdapter {
  /// Creates a placeholder adapter.
  const RelaySyncHttpAdapter();
}
