name: relay_sync_sqflite
description: Sqflite storage adapter for relay_sync.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  relay_sync:
    path: ../relay_sync

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
