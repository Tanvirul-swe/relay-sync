name: relay_sync
description: Pure Dart core for offline API queueing and sync orchestration.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
